This code will extract names, organizations and locations using Stanford NER tagger. 
Also present is a simple pattern matching based attempt to find job titles from raw text. The list of job titles is extracted from Naukri.
A distance based approach to match each name with job title also is present.

Packages to be installed:
nltk

Steps to run:
	1. Cd to location of the code.
	2. python run_job_name.py text_to_tag
	3. If no text is provided, it will run with default text in the code.